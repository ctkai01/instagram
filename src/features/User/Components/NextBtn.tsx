import * as React from 'react';

export interface INextBtnProps {
}

export default function NextBtn (props: INextBtnProps) {
  return (
    <div style={{ width: '20px', height: '20px', background: 'red' }}>
      
    </div>
  );
}
